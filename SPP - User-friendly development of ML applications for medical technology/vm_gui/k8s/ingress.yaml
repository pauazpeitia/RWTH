apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spp-monolith
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spp-monolith
                port:
                  number: 8000
